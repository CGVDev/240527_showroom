import styled from "styled-components";
import { Modal } from "./Modal";

const ModalApartementsList = ()=>{
    return(
        <Modal title="">

        </Modal>
    );
};

const ModalContainer = styled.div`
    width: 100%;
`;